# API Configuration
# Base URL for the Flow Builder API
# In development, this is used by Vite's proxy
# In production, this should be the full API URL (e.g., https://api.example.com)
VITE_API_BASE_URL=http://localhost:8003

# Base path for the application (default: /)
# Set this when deploying behind a reverse proxy with a subpath
# Example: /flow-builder for https://example.com/flow-builder
VITE_BASE_PATH=/
